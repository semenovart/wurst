import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Заголовки безопасности по умолчанию; кэш статики Vercel настраивает сам.
  poweredByHeader: false,
};

export default nextConfig;
