/**
 * ЕДИНСТВЕННОЕ место персонализации свадьбы.
 * Перед запуском замените плейсхолдеры на реальные данные пары.
 *
 * В v2 (публичная версия) этот объект станет записью в БД,
 * а `slug` — частью URL вида /w/masha-i-dima.
 */
export const wedding = {
  /** Имена пары в именительном падеже: «Маша и Дима женятся» */
  coupleLabel: "Маша и Дима",
  /** Имена пары в родительном падеже: «свадьба Маши и Димы» */
  coupleGenitive: "Маши и Димы",
  /** Дата и время начала свадьбы (с таймзоной города) */
  dateISO: "2026-09-12T14:00:00+03:00",
  city: "Санкт-Петербург",
  /** Scope ключей в Redis; в v1 не менять */
  slug: "default",
} as const;

export type WeddingConfig = typeof wedding;
