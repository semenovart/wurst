/**
 * Транзитные (покадровые) данные взаимодействия — мимо React.
 * Пишутся обработчиками указателя, читаются в useFrame.
 */
export const hoverSpot = {
  x: 0,
  z: 0,
  /** Указатель сейчас над валидным местом для ямы */
  active: false,
};
